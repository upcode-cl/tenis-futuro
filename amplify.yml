version: 1
frontend:
  phases:
    preBuild:
      commands:
        - corepack enable
        - pnpm install --frozen-lockfile
    build:
      commands:
        # Amplify inyecta vars en el build, pero Next.js SSR no las ve en runtime
        # sin .env.production. Ver: https://docs.aws.amazon.com/amplify/latest/userguide/ssr-environment-variables.html
        - touch .env.production
        # No copiar AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY: Amplify las usa
        # para el build. Las credenciales S3 deben ir como NEXT_AWS_*.
        - env | grep -E '^(MONGODB_URI|MONGODB_DB_NAME|ADMIN_SESSION_SECRET|ADMIN_USER|ADMIN_PASSWORD|S3_BUCKET_NAME|S3_PUBLIC_URL_BASE|NEXT_AWS_ACCESS_KEY_ID|NEXT_AWS_SECRET_ACCESS_KEY|NEXT_AWS_REGION|INSTAGRAM_ACCESS_TOKEN|INSTAGRAM_USER_ID)=' >> .env.production || true
        - env | grep -E '^NEXT_PUBLIC_' >> .env.production || true
        # Webpack evita el runtime Turbopack en Lambda (más estable con mongodb en Amplify)
        - pnpm run build
        # Si Turbopack generó .next/node_modules, copia deps faltantes (bson, etc.)
        - node scripts/resolve-next-symlinks.js
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
